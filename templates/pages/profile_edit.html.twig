<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier mon profil - Lakers Newz</title>
    <link href="{{ asset('assets/fonts/fontawesome/css/fontawesome.min.css') }}" rel="stylesheet">
    <link href="{{ asset('assets/fonts/fontawesome/css/brands.min.css') }}" rel="stylesheet">
    <link href="{{ asset('assets/fonts/fontawesome/css/solid.css') }}" rel="stylesheet">
    <link href="{{ asset('assets/fonts/fontawesome/css/solid.min.css') }}" rel="stylesheet">
    <link rel="shortcut icon" href="{{ asset('assets/icons/favicon.ico') }}" type="image/x-icon">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Exo:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/profile.css') }}">
</head>
<body class="profil-page">
    <div class="profil-fond-motif"></div>

    <div class="profil-vue">
        <header class="profil-entete">
            <h1 class="profil-entete-titre">Modifier mes informations</h1>
            <button class="profil-bouton-retour" onclick="window.location.href='{{ path('app_profil') }}'"><i class="fa-solid fa-chevron-left"></i></button>
        </header>

        {{ form_start(profilForm, {'attr': {'class': 'info-zone'}}) }}

            <div class="info-zone">
                <h2 class="info-zone-titre">Mon identité</h2>
                <div class="info-tableau">
                    <div class="info-ligne">
                        <span class="info-libelle">{{ form_label(profilForm.lastname) }}</span>
                        {{ form_widget(profilForm.lastname, {'attr': {'class': 'info-modifier-champ'}}) }}
                    </div>
                    <div class="info-ligne">
                        <span class="info-libelle">{{ form_label(profilForm.firstname) }}</span>
                        {{ form_widget(profilForm.firstname, {'attr': {'class': 'info-modifier-champ'}}) }}
                    </div>
                    <div class="info-ligne">
                        <span class="info-libelle">{{ form_label(profilForm.pseudo) }}</span>
                        {{ form_widget(profilForm.pseudo, {'attr': {'class': 'info-modifier-champ'}}) }}
                    </div>
                    <div class="info-ligne">
                        <span class="info-libelle">{{ form_label(profilForm.dateNaissance) }}</span>
                        {{ form_widget(profilForm.dateNaissance, {'attr': {'class': 'info-modifier-champ'}}) }}
                    </div>
                    <div class="info-ligne">
                        <span class="info-libelle">{{ form_label(profilForm.lieuNaissance) }}</span>
                        {{ form_widget(profilForm.lieuNaissance, {'attr': {'class': 'info-modifier-champ'}}) }}
                    </div>
                </div>
            </div>

            <div class="info-zone">
                <h2 class="info-zone-titre">Mes informations de contact</h2>
                <div class="info-tableau">
                    <div class="info-ligne">
                        <span class="info-libelle">{{ form_label(profilForm.email) }}</span>
                        {{ form_widget(profilForm.email, {'attr': {'class': 'info-modifier-champ'}}) }}
                    </div>
                    <div class="info-ligne">
                        <span class="info-libelle">{{ form_label(profilForm.telephone) }}</span>
                        {{ form_widget(profilForm.telephone, {'attr': {'class': 'info-modifier-champ'}}) }}
                    </div>
                </div>
            </div>

            <button type="submit" class="profil-info-bouton">
                <i class="fa-solid fa-floppy-disk"></i> Enregistrer
            </button>

        {{ form_end(profilForm) }}
    </div>
</body>
</html>
