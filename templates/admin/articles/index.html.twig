{% extends 'base.html.twig' %}
{% block title %}Admin - Articles{% endblock %}
{% block stylesheets %}<link rel="stylesheet" href="{{ asset('css/admin.css') }}">{% endblock %}
{% block body_class %}admin-page{% endblock %}
{% block body %}
<main class="admin-main">
    <div class="admin-header">
        <h1 class="admin-title">Gestion des articles</h1>
        <a href="{{ path('admin_articles_new') }}" class="admin-btn-new">+ Nouvel article</a>
    </div>

    {% include 'admin/_nav.html.twig' %}

    {% for message in app.flashes('success') %}
        <div class="admin-flash-success">{{ message }}</div>
    {% endfor %}

    <div class="admin-table-wrapper">
        <table class="admin-table">
            <thead>
                <tr>
                    <th>Titre</th>
                    <th class="center">Catégorie</th>
                    <th class="center">Auteur</th>
                    <th class="center">Date</th>
                    <th class="center">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for article in articles %}
                    <tr>
                        <td>{{ article.titre }}</td>
                        <td class="center">{{ article.categorie ?? '—' }}</td>
                        <td class="center">{{ article.auteur ?? 'Lakers Newz' }}</td>
                        <td class="center">{{ article.createdAt ? article.createdAt|date('d/m/Y') : '—' }}</td>
                        <td class="center">
                            <a href="{{ path('admin_articles_edit', {id: article.id}) }}" class="admin-btn-edit">Modifier</a>
                            <form method="post" action="{{ path('admin_articles_delete', {id: article.id}) }}" style="display: inline;" onsubmit="return confirm('Supprimer cet article ?');">
                                <button type="submit" class="admin-btn-delete">Supprimer</button>
                            </form>
                        </td>
                    </tr>
                {% else %}
                    <tr><td colspan="5" class="empty-row">Aucun article</td></tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</main>
{% endblock %}
